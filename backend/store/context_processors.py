def cart(request):
    """
    بج تعداد سبد خرید در هدر (در حال حاضر منطق سبد خرید سمت کلاینت
    و در فایل static-src/js/modules/cartStore.js مدیریت می‌شود).
    این context processor محل آماده برای وقتی‌ست که سبد خرید به بک‌اند
    و مدل‌های Order/OrderItem وصل شود.
    """
    return {}
